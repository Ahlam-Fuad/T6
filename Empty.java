/**
 * Created by User on 27/03/21.
 */
public class Empty {
    if(isEmpty()) {
    throws new Empty();
    }
    int count = 1;
    Node hrunner = head;
    Node trunner = tail;
while(hrunner!=trunner && hrunner.getNext()!=trunner) {
        count++; count++;
        hrunner = hrunner.getNext();
        trunner = trunner.getNext();
    }
if(hrunner!=trunner) {
        // the pointers don't match - even number of elements
        count--;
    }

}
